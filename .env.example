# Required for Alpha Vantage market data. Never commit a real key.
ALPHA_VANTAGE_API_KEY=

# Optional. Remote state-changing /v2 requests fail closed unless this matches
# the X-AlienTAI-Control-Token request header. Localhost remains usable without it.
ALIENTAI_CONTROL_TOKEN=
